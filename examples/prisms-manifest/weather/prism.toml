# Weather Prism Manifest
# This file enables manifest-based discovery for the weather prism

[prism]
name = "weather"
version = "1.0.0"
binary = "shine-weather"
description = "Weather widget with icons and auto-refresh"
author = "Shine Team <team@shine.example.com>"
license = "MIT"

[dependencies]
requires = ["shine >= 0.2.0"]

[metadata]
homepage = "https://github.com/starbased-co/shine"
repository = "https://github.com/starbased-co/shine"
tags = ["weather", "widget", "status-bar"]
